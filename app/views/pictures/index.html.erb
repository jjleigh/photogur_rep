
 
    <div class="row">
    	<div class="large-12 small-centered columns">
				<h1 class="text-center index-head">Most popular pictures</h1>

    	</div>
    </div>


  <!-- Here the pictures begin -->
  <div class="row">
      
    <div class="large-12 columns">
      

      <div class="panel index-panel">
        <div class="row">
          <% @pictures.each do |picture| %>
        
            <div class="large-4 small-6 columns">
              <div class="blur-img">
                <%= link_to image_tag(picture.url), "/pictures/#{picture.id}", :class => 'index-image' %>
              </div>
              

              <div class="panel">
                <h5><%= picture.title %></h5>
                <h6 class="subheader"><%= picture[:artist] %></h6>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    
    </div>
  </div>
 
 
    
 
     
 
    </div>
  </div>
    <!--    
        
          	<%# @pictures.each do |picture| %>
							<ul class="small-block-grid-4">
								
							  <li>
							  	<%#= link_to image_tag(picture.url), "/pictures/#{picture.id}", :id => 'index-image' %>
									
							  	<div class="panel index-image-panel">
  									<h5><%#= picture.title %></h5>
  									<h6 class="subheader"><%#= picture[:artist] %></h6>
  								</div>
							  </li>
							</ul>

				<%# end %>
             -->